{
  <#if name??> "name": "${name}",</#if>
  <#if key??> "key": "${key}",</#if>
  <#if logoKey??> "logoKey": "${logoKey}",</#if>
  <#if publiclyAccessible??> "publiclyAccessible": ${publiclyAccessible},</#if>
  "description": "${description}"
}

