{
  <#if sessionId??>"sessionId": "${sessionId}",</#if>
  <#if startedAt??>"startedAt": "${startedAt}",</#if>
  <#if capabilities??> "capabilities": <#include "*/session_controller/capabilities.json">, </#if>
  <#if desiredCapabilities??> "desiredCapabilities": <#include "*/session_controller/desireCapabilities.json">,</#if>
  "testIds": ${testId}
}