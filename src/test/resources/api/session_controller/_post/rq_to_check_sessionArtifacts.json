{
  <#if sessionId??>"sessionId": "${sessionId}",</#if>
  <#if startedAt??>"startedAt": "${startedAt}",</#if>
  "capabilities": {
    "enableVideo": true,
    "videoLink": "videoLink",
    "enableLog":true,
    "logLink":"logLink",
    "enableMetadata":true,
    "metadataLink":"metadataLink",
    "enableVNC":true,
    "proxy": {},
    "slotCapabilities": {},
    "desired": { }
  },
   "desiredCapabilities": {
     <#if metadataLink??>  "metadataLink": "${metadataLink}",</#if>
     <#if logLink??> "logLink": "${logLink}",</#if>
     <#if videoLink??> "videoLink": "${videoLink}",</#if>
     <#if enableVideo??> "enableVideo": ${enableVideo},</#if>
     <#if enableLog??> "enableLog":${enableLog},</#if>
     <#if enableMetadata??>  "enableMetadata": ${enableMetadata},</#if>
     <#if enableVNC??> "enableVNC": ${enableVNC},</#if>
     "proxy": {}
},
  "testIds": ${testId}
}